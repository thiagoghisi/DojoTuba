Para rodar o projeto, é necessário:

$ gem install bundler
$ cd dojoNumerosFelizes
$ bundle install

E, para rodar os specs:

$ bundle exec rspec spec/numeros_spec.rb

* Dica By: André Luis Junior (@andrehjr)
